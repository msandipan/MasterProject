This directory is for running patch classifier
Before running the code create a directory with a folder /models 
For training run: self_supervised_test.py
The arguments are: -GPU name(eg. gpu0) -number of epochs -Task(eg. task1)
The Tasks can be viewed in patch_classifiers/cfgs/10CV.py  

